import wollok.game.*
import main.*
import enemigos.*
import config.*


program juego {
	config.configurarTeclas()
	game.title("Juego Base")
	game.height(10)
	game.width(10)
	game.addVisual(main)
	game.addVisual(enemy1)
	game.boardGround("bg1.png")
	game.start()
}