import wollok.game.*
import nave.*
import niveles.*

program juego {
	game.title("Juego Base")
	game.height(10)
	game.width(20)
	game.boardGround("fondo.jpg")
	tutorial1.iniciar()
	game.showAttributes(enemigo)
	game.start()
}

